/*import { Router } from 'express';
import { getAllChallenges, createChallenge, updateChallenge, deleteChallenge,getUsersChallenge, getChallengesByCreatorId} from '../controllers/challenge.controller';
import { get } from 'http';

const router = Router();

router.get('/', getAllChallenges);
router.get('/users', getUsersChallenge);
router.get('/:id',getChallengesByCreatorId);
router.post('/', createChallenge);
router.put('/:id', updateChallenge);
router.delete('/:id', deleteChallenge);

export default router; */